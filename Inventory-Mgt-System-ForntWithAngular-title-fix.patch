diff --git a/Inventory-Mgt-System-ForntWithAngular-ci-fix.patch b/Inventory-Mgt-System-ForntWithAngular-ci-fix.patch
deleted file mode 100644
index 1e91569..0000000
--- a/Inventory-Mgt-System-ForntWithAngular-ci-fix.patch
+++ /dev/null
@@ -1,209 +0,0 @@
-diff --git a/Inventory-Mgt-System-ForntWithAngular b/Inventory-Mgt-System-ForntWithAngular
-deleted file mode 160000
-index 8682948..0000000
---- a/Inventory-Mgt-System-ForntWithAngular
-+++ /dev/null
-@@ -1 +0,0 @@
--Subproject commit 86829488fb19da50ddc02197e062ce2fd34c4c6f
-diff --git a/Inventory-Mgt-System-ForntWithAngular-ci.patch b/Inventory-Mgt-System-ForntWithAngular-ci.patch
-deleted file mode 100644
-index 1c2f0cf..0000000
---- a/Inventory-Mgt-System-ForntWithAngular-ci.patch
-+++ /dev/null
-@@ -1,35 +0,0 @@
--diff --git a/.github/workflows/ci.yml b/.github/workflows/ci.yml
--new file mode 100644
--index 0000000..675d8d5
----- /dev/null
--+++ b/.github/workflows/ci.yml
--@@ -0,0 +1,29 @@
--+name: CI
--+
--+on:
--+  push:
--+    branches: [ main, master ]
--+  pull_request:
--+    branches: [ main, master ]
--+
--+jobs:
--+  build-and-test:
--+    runs-on: ubuntu-latest
--+    steps:
--+      - name: Checkout
--+        uses: actions/checkout@v4
--+
--+      - name: Set up Node.js
--+        uses: actions/setup-node@v4
--+        with:
--+          node-version: '20'
--+          cache: npm
--+
--+      - name: Install dependencies
--+        run: npm ci
--+
--+      - name: Run unit tests
--+        run: npx ng test --watch=false --browsers=ChromeHeadless
--+
--+      - name: Build
--+        run: npx ng build
-diff --git a/Inventory-Mgt-System-ForntWithAngular-fixes.patch b/Inventory-Mgt-System-ForntWithAngular-fixes.patch
-deleted file mode 100644
-index 777040c..0000000
---- a/Inventory-Mgt-System-ForntWithAngular-fixes.patch
-+++ /dev/null
-@@ -1,22 +0,0 @@
--diff --git a/src/app/service/api.service.ts b/src/app/service/api.service.ts
--index f2de147..c6cd308 100644
----- a/src/app/service/api.service.ts
--+++ b/src/app/service/api.service.ts
--@@ -141,7 +141,7 @@ export class ApiService {
--     });
--   }
--   updateProduct(formData:any):Observable<any>{
---    return this.http.put('${ApiService.BASE_URL}/products/update/',formData,{
--+    return this.http.put(`${ApiService.BASE_URL}/products/update`,formData,{
--       headers: this.getHeader(),
--     });
--   }
--@@ -159,7 +159,7 @@ export class ApiService {
--   }
--   getAlltransactions(searchText: string):Observable<any>{
--     return this.http.get(`${ApiService.BASE_URL}/transactions/all`,{
---      params: { searchText: searchText },
--+      params: { searchBox: searchText },
--       headers: this.getHeader(),
--     });
--   }
-diff --git a/Inventory-Mgt-System-ForntWithAngular-tests.patch b/Inventory-Mgt-System-ForntWithAngular-tests.patch
-deleted file mode 100644
-index aa1d332..0000000
---- a/Inventory-Mgt-System-ForntWithAngular-tests.patch
-+++ /dev/null
-@@ -1,67 +0,0 @@
--diff --git a/src/app/service/api.service.spec.ts b/src/app/service/api.service.spec.ts
--index c0310ae..584bed4 100644
----- a/src/app/service/api.service.spec.ts
--+++ b/src/app/service/api.service.spec.ts
--@@ -1,16 +1,61 @@
-- import { TestBed } from '@angular/core/testing';
--+import { HttpTestingController, provideHttpClientTesting } from '@angular/common/http/testing';
--+import { provideHttpClient } from '@angular/common/http';
-- 
-- import { ApiService } from './api.service';
-- 
--+/**
--+ * Regression tests for ApiService.
--+ *
--+ * Two real bugs are covered here:
--+ *  - updateProduct() built its URL with single quotes instead of a
--+ *    template literal, so `${ApiService.BASE_URL}/products/update` was
--+ *    sent to the server literally instead of being interpolated — editing
--+ *    a product from the UI never actually reached the backend.
--+ *  - getAlltransactions() sent a `searchText` query param, but the
--+ *    backend's /transactions/all endpoint only reads `searchBox`, so the
--+ *    transaction search filter silently did nothing.
--+ */
-- describe('ApiService', () => {
--   let service: ApiService;
--+  let httpMock: HttpTestingController;
-- 
--   beforeEach(() => {
---    TestBed.configureTestingModule({});
--+    TestBed.configureTestingModule({
--+      providers: [provideHttpClient(), provideHttpClientTesting()]
--+    });
--     service = TestBed.inject(ApiService);
--+    httpMock = TestBed.inject(HttpTestingController);
--+  });
--+
--+  afterEach(() => {
--+    httpMock.verify();
--   });
-- 
--   it('should be created', () => {
--     expect(service).toBeTruthy();
--   });
--+
--+  it('updateProduct sends a PUT to the real, interpolated backend URL', () => {
--+    const formData = new FormData();
--+
--+    service.updateProduct(formData).subscribe();
--+
--+    const req = httpMock.expectOne(
--+      (r) => r.method === 'PUT' && r.url === 'http://localhost:8080/api/products/update'
--+    );
--+    expect(req.request.url).not.toContain('${ApiService.BASE_URL}');
--+    req.flush({ status: 200 });
--+  });
--+
--+  it('getAlltransactions sends the search text as "searchBox", matching the backend param name', () => {
--+    service.getAlltransactions('laptop').subscribe();
--+
--+    const req = httpMock.expectOne(
--+      (r) => r.method === 'GET' && r.url === 'http://localhost:8080/api/transactions/all'
--+    );
--+    expect(req.request.params.get('searchBox')).toBe('laptop');
--+    expect(req.request.params.has('searchText')).toBeFalse();
--+    req.flush({ status: 200 });
--+  });
-- });
-diff --git a/src/app/add-edit-product/add-edit-product.component.ts b/src/app/add-edit-product/add-edit-product.component.ts
-index dfc0797..dc6d245 100644
---- a/src/app/add-edit-product/add-edit-product.component.ts
-+++ b/src/app/add-edit-product/add-edit-product.component.ts
-@@ -3,7 +3,6 @@ import { Component, OnInit } from '@angular/core';
- import { FormsModule } from '@angular/forms';
- import { ApiService } from '../service/api.service';
- import { ActivatedRoute, Router } from '@angular/router';
--import { timeStamp } from 'console';
- 
- @Component({
-   selector: 'app-add-edit-product',
-diff --git a/src/app/category/category.component.ts b/src/app/category/category.component.ts
-index d969d20..7536db3 100644
---- a/src/app/category/category.component.ts
-+++ b/src/app/category/category.component.ts
-@@ -2,7 +2,6 @@ import { CommonModule } from '@angular/common';
- import { Component, OnInit } from '@angular/core';
- import { FormsModule } from '@angular/forms';
- import { ApiService } from '../service/api.service';
--import { error } from 'console';
- 
- interface Category{
-   id:string,
-diff --git a/src/app/login/login.component.ts b/src/app/login/login.component.ts
-index 57cc69c..8f479c5 100644
---- a/src/app/login/login.component.ts
-+++ b/src/app/login/login.component.ts
-@@ -4,7 +4,6 @@ import { Router, RouterLink } from '@angular/router';
- import { FormsModule } from '@angular/forms';
- import { ApiService } from '../service/api.service';
- import { firstValueFrom } from 'rxjs';
--import { error } from 'console';
- 
- @Component({
-   selector: 'app-login',
-diff --git a/src/app/register/register.component.ts b/src/app/register/register.component.ts
-index b8b2656..cd089c2 100644
---- a/src/app/register/register.component.ts
-+++ b/src/app/register/register.component.ts
-@@ -4,7 +4,6 @@ import { Router, RouterLink } from '@angular/router';
- import { FormsModule } from '@angular/forms';
- import { ApiService } from '../service/api.service';
- import { firstValueFrom } from 'rxjs';
--import { error } from 'console';
- 
- @Component({
-   selector: 'app-register',
-diff --git a/src/app/service/api.service.ts b/src/app/service/api.service.ts
-index c6cd308..18add47 100644
---- a/src/app/service/api.service.ts
-+++ b/src/app/service/api.service.ts
-@@ -4,7 +4,6 @@ import CryptoJS from 'crypto-js';
- import { Observable } from 'rxjs';
- import { HttpClient, HttpHeaders } from '@angular/common/http';
- import { EventEmitter } from '@angular/core';
--import { stat } from 'fs';
- 
- @Injectable({
-   providedIn: 'root'
diff --git a/src/app/app.component.spec.ts b/src/app/app.component.spec.ts
index 42e5e91..962bd98 100644
--- a/src/app/app.component.spec.ts
+++ b/src/app/app.component.spec.ts
@@ -18,16 +18,16 @@ describe('AppComponent', () => {
     expect(app).toBeTruthy();
   });
 
-  it(`should have the 'shipping-frontend' title`, () => {
+  it(`should have the 'shipping' title`, () => {
     const fixture = TestBed.createComponent(AppComponent);
     const app = fixture.componentInstance;
-    expect(app.title).toEqual('shipping-frontend');
+    expect(app.title).toEqual('shipping');
   });
 
-  it('should render title', () => {
+  it('should render the sidebar title', () => {
     const fixture = TestBed.createComponent(AppComponent);
     fixture.detectChanges();
     const compiled = fixture.nativeElement as HTMLElement;
-    expect(compiled.querySelector('h1')?.textContent).toContain('Hello, shipping-frontend');
+    expect(compiled.querySelector('h1')?.textContent).toContain('Shipping');
   });
 });
